# Checkee 项目全面优化方向审查

> 审查日期：2026-09-07  
> 审查方式：静态读取当前工作区代码、配置、数据快照与项目文档。  
> 本报告未修改产品代码、未运行 build、未进行浏览器或真机测试。移动端结论均来自 390px/760px CSS 规则与组件结构审查，需要在决定实施后再做一次实际视口验证。

## 1. 当前项目状态总结

Checkee 已经越过“功能原型”阶段，进入产品化收尾与可靠性治理阶段。它目前不是单纯的静态展示页，而是一套具有明确边界的公开案例归档系统：

- `/` 已形成 Top 3 荣誉展示、4–10 精选展柜、11+ 档案列表的稳定信息层级；
- `/view` 已形成五城样本概览、Q1/Median/Q3、月度趋势与城市案例浏览；
- `/about` 保持独立的个人表达，不与数据产品混杂；
- 公开投稿经过服务端校验进入 Supabase 审核池；
- Admin 已覆盖登录、投稿审核、发布状态、Release 历史和版本 Diff；
- Hall 通过静态主数据、不可覆盖 Release 和校验脚本维持可追溯发布；
- CI 已包含 lint、typecheck、build 与 Hall 数据验证。

当前 Hall 快照包含 97 条记录，版本为 `20260907-v002`；View 快照包含 503 条记录，快照日期为 2026-09-01，覆盖 2026 年 1–8 月，并明确标记为非实时数据。产品核心闭环已经存在，下一阶段不应扩张页面或功能数量，而应优先解决可信度表达、投稿安全、首访认知、移动端可读性和资源体积。

### `/view` 当前实现判断

`/view` 的产品角色是“冷静的数据观察空间”，不是 Dashboard。当前五张城市卡片承担入口，左侧月度趋势与右侧城市案例构成主要工作面；移动端改为两列城市入口、单列详情和卡片化表格。结构本身合理，没有证据支持增加更多图表或筛选器。

但有三个值得关注的真实问题：

1. 标题写死为“2026.1-8月”，而数据本身已有 `snapshotDate`、`isLive` 与月份范围，未来更新容易产生文案漂移；
2. 数据说明与更新时间藏在详情 Popover 内，首次进入时无法立即看到“样本、快照、非实时”的可信度摘要；
3. Hall 和 View 共用一个大型客户端组件，并同时接收两套 JSON；这使页面职责、数据负载和 CSS 难以继续独立演进。

## 2. 已经做得好的地方

### 产品设计

- **定位清晰。** `docs/CHECKEE_STATE.md` 已明确“东方档案展而非普通仪表盘”，并把“如无必要，勿增实体”写成项目约束。
- **层级有意义。** Top 3、4–10、11+ 对应荣誉、精选和档案三种浏览心智，不是单纯用尺寸制造层级。
- **入口克制。** Hall 仅保留“提交案例”和“修改/说明”两个操作，避免 CTA 堆叠。
- **View 没有过度分析化。** 当前只展示对用户有解释价值的分位数、月度趋势和案例，没有为了丰富感而堆砌图表。
- **公开展示与后台管理分离。** Admin 不进入公开导航或 sitemap，符合产品边界。

### UI / UX

- Top 3、Divider、4–10、11+ 已形成连续的东方展厅叙事，并对移动端取消冠军抬升，避免机械缩放桌面构图。
- Hall 与 View 使用不同的页面背景变量，About 也保持独立人格，页面间已有明确空间区分。
- 提交表单具备字段级错误、提交中、成功、失败、键盘焦点和 `aria-live`；Dialog 使用 Headless UI，基础可访问性好于手写浮层。
- 城市卡片使用 `aria-pressed`，Top 3 可展开备注支持键盘 Enter/Space，分页按钮有禁用状态。
- 已实现 `prefers-reduced-motion`，且动效主要用于进入、悬停和状态变化，没有持续闪烁或游戏化效果。
- 移动端不是简单压缩桌面布局：城市卡片重排、趋势表改为卡片行、Hall 改为冠军单列加二三名双列。

### 数据可信度

- Hall 只展示 `visibility=published`，投稿不会直接进入前台。
- 服务端重新执行必填、长度、枚举、日期合法性和日期先后校验，不依赖前端校验。
- `waitingDays` 对进行中和已结束案例采用不同计算边界；缺少结束日期的非 Check 历史记录不会随当前日期漂移。
- 数据来源、发布快照、Release 版本与校验脚本已形成清晰链路，且历史 Release 不覆盖。
- Admin 更新时附带 `visibility=pending` 条件，可识别记录已被其他管理员处理的冲突。

### 工程质量

- TypeScript 开启 strict，核心数据结构有集中类型定义。
- Supabase Secret 只在服务端读取；Admin Cookie 使用 HttpOnly、SameSite Strict、生产环境 Secure 和 HMAC 签名。
- Next.js 安全响应头包含 CSP、HSTS、X-Frame-Options、Referrer Policy 等基础防护。
- 关键数据生成与验证脚本职责分离，CI 已把数据验证纳入质量门禁。
- Release Diff 是只读能力，没有把高风险发布动作塞进网页按钮，符合当前规模下的稳健策略。

## 3. 优化机会列表

| 方向 | 问题 | 建议方向 | 收益 | 实现成本 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| 安全 / 数据质量 | 公开 `/api/submissions` 可直接写入高权限 Supabase 客户端，目前没有限流、蜜罐、重复案例检测或请求体体积上限 | 先做低摩擦分层防护：请求体上限、速率限制、蜜罐/时间陷阱、重复候选标记；只有真实滥用出现后再考虑验证码 | 防止垃圾投稿、数据库成本和审核池污染 | 中 | P0 |
| Admin 安全 | 密码登录端点没有失败次数限制；若 `/admin` 暴露公网，可被持续尝试 | 增加登录速率限制、短期失败冷却和安全日志；如部署环境允许，可再加平台访问保护 | 降低口令暴力尝试风险 | 中 | P0 |
| 用户贡献 / 隐私 | 投稿包含城市、学校、专业和精确日期，这些字段组合可能具有可识别性；表单没有明确说明哪些字段会公开、如何整理、是否可修改 | 在提交前给出极短的公开范围、审核流程和敏感信息提醒；让用户确认理解，而不是增加长协议 | 提升信任，减少隐私争议和撤回成本 | 低 | P0 |
| 性能 | `HYBlackMythU.woff2` 约 8.6MB，且导航和多页标题都使用该字体，属于全站高概率资源 | 对实际使用字符做字体子集，或限制到少量标题并设置可靠回退；保留现有东方字体气质 | 显著改善移动网络首访与字体切换 | 中 | P0 |
| 首次访问 | Hall 第一屏只有“2026年度白宫严选中国硕博”和视觉展陈；产品用途、样本属性、非官方性质与参与方式主要藏在弹窗/控件中 | 不新增独立板块，只在现有标题或控制区附近提供一条极短的可信度摘要，例如样本性质、快照日期、案例数 | 降低首次访问认知成本，不破坏展厅构图 | 低 | P1 |
| 数据可信度 | Hall 数据对象已有版本、生成时间、快照日期、记录数和来源，但前台仅在说明中显示快照日期；View 的 `isLive=false` 也未显式表达 | 统一“数据截至 / 样本量 / 非官方 / 非实时”表达，详细方法仍留在 Popover | 用户更容易判断数据适用范围 | 低 | P1 |
| 数据文案 | `/view` 标题硬编码“2026.1-8月”，Hall 标题硬编码年度；数据已包含月份范围与快照日期 | 让展示文案从快照元数据派生，避免更新数据后忘记改标题 | 减少过期文案和可信度损耗 | 低 | P1 |
| 提交流程 | 客户端把所有非 2xx 响应统一成“提交失败，请稍后再试”，服务端 400/500 也返回相同文案 | 保持简洁，但区分“内容需修正”“网络异常”“服务暂不可用”；失败时继续保留表单内容并提供重试 | 提升可恢复性，减少重复填写 | 低 | P1 |
| 提交闭环 | 成功页只说明“整理后进入名人堂”，没有明确“先审核、不会立即显示、如何修改” | 成功状态补全三步预期：已收到、待审核、发布后可见，并保留修改/联系路径 | 降低用户对即时上线的误解 | 低 | P1 |
| 投稿去重 | 服务端没有基于城市、日期、学校、专业等生成重复候选，也没有幂等标识；重复点击或重复投稿只能靠人工识别 | 不自动拒绝，只在审核池标记“可能重复”，保留人工判断 | 提升数据质量且不误伤真实相似案例 | 中 | P1 |
| 客户端负载 | `CheckmatePage` 同时导入约 345KB View 快照和约 45KB Hall 数据，再一起传给 `'use client'` 的 `CheckmateExperience`；每个页面都承担不需要的数据 | 按 Hall/View 拆分服务端数据入口与最小客户端岛，仅把当前页面需要的数据传到浏览器 | 降低传输、解析和 hydration 成本 | 中 | P1 |
| 工程结构 | `CheckmateExperience` 约 639 行，CSS Module 约 3200 行/78KB，两个产品页面、Dialog、状态和大量响应式规则集中在同一边界 | 按“Hall / View / Shared primitives”做有限拆分；只拆稳定职责，不创建抽象组件体系 | 降低回归风险，便于独立演进 | 中 | P1 |
| 移动端 / 表单 | 760px 以下投稿 Dialog 被设为 `border-radius: 50%`，同时 8 个字段改为单列并在面板内滚动；在 390px 上容易形成椭圆裁切感和过长的内部滚动 | 保留小型弹窗方向，但移动端使用适度圆角、稳定顶部标题/底部操作区，并减少滚动嵌套感 | 提升核心贡献流程完成率 | 低至中 | P1 |
| 移动端 / 可读性 | 多处关键或辅助信息低于 12px：如 `0.58rem`、`0.62rem`、`0.66rem`、`0.68rem`；移动趋势标签也存在 `0.58rem` | 建立字体下限：常用标签约 14px，次要元数据至少 12px；优先处理表单、状态、日期和趋势标签 | 改善 390px 与高缩放可读性 | 中 | P1 |
| 移动端 / 首屏 | Hall 移动布局为冠军单列、操作按钮纵向、二三名双列；结构成立，但标题 + 冠军 + 两个按钮会占用较长首屏 | 实施前做 390×844 真机/浏览器验证，重点判断首屏是否至少露出下一层级的视觉线索；只调间距，不重构 Top 3 | 保持仪式感同时建立可滚动预期 | 低 | P1（待视觉验证） |
| 可访问性 | 月度趋势用 `role=table` 和 `role=row`，但内部单元格没有 rowheader/cell/columnheader 语义；移动端又隐藏视觉表头 | 使用完整表格语义或补齐 ARIA 单元格关系，同时保留当前视觉表现 | 屏幕阅读器能理解行列关系 | 低至中 | P1 |
| SEO / 分享 | 全站 description 为“个人主页与 Checkmate 信息展示”，没有体现 F-1 时间线案例；页面未提供专属 description | 为 Hall、View、About 提供各自的搜索摘要，突出公开样本、城市范围、非官方性质 | 提升搜索结果可理解性 | 低 | P1 |
| SEO / 分享 | Open Graph 与 Twitter 图片数组为空，但 Twitter card 声明为 `summary_large_image` | 只有在准备公开传播时制作一张克制的品牌分享图；不要为每条案例生成图片 | 提升 GitHub、社交平台分享识别度 | 低至中 | P1 |
| SEO | `genPageMetadata` 固定 `locale: en_US`，与站点 `zh-CN` 不一致 | 统一为站点 locale，并检查各路由 canonical/OG URL | 修正爬虫与分享语义 | 低 | P1 |
| 质量保障 | 未发现自动化测试；当前 CI 能发现类型、构建和数据 schema 问题，但不能验证排序、等待天数、表单校验和审核状态转换行为 | 仅为高风险纯函数和 API 边界增加少量测试：日期、排序、状态归一化、重复/非法提交、审核冲突 | 防止数据口径回归 | 中 | P1 |
| 配置维护 | README 要求配置 `SUPABASE_URL` 与 `SUPABASE_SECRET_KEY`，但 `.env.example` 未列出；README 同时出现 `source=submission` 与正确值 `submission_user` | 对齐示例环境变量和来源命名 | 降低部署与交接错误 | 低 | P1 |
| Hall 分页 | 翻页只替换 10 条记录，不更新 URL，也不管理翻页后的阅读位置或结果播报 | 先观察使用数据；若深页访问明显，再增加轻量的页码状态、位置恢复和 `aria-live`，不必先做搜索 | 改善长列表连续浏览 | 低至中 | P2 |
| 联系反馈 | “打开邮件客户端”依赖本机 mail handler，网页无法确认是否成功；失败后没有复制兜底 | 保留 mailto 低成本方案，同时提供复制邮箱/说明的回退 | 提高修改反馈可达性 | 低 | P2 |
| SEO | 没有结构化数据 | 仅在内容定义稳定后考虑 `WebSite` / `Dataset` 级结构化数据；不要给单条案例建立公开详情页 | 可能改善搜索理解 | 低至中 | P2 |
| Sitemap | `lastModified` 使用每次生成 sitemap 的当前日期，而不是页面或数据实际更新时间 | Hall/View 使用对应快照或生成日期，About 使用稳定内容更新时间 | 避免向搜索引擎发送虚假更新信号 | 低 | P2 |
| 一致性 | 404 页面仍是英文模板，通用 `Button` 保留亮蓝默认风格；后台中英文状态标签混用 | 在不触碰核心 UI 的前提下，逐步统一中文语气与状态命名 | 减少旧模板残留感 | 低 | P2 |
| 代码清理 | `ThemeSwitcher` 存在但 Header 未渲染，`body-scroll-lock` 未发现引用，部分旧通用组件风格不再匹配 Checkee | 确认是否仍有未来用途后再删除；不要为“代码洁癖”立即清理 | 减少无效维护面 | 低 | P2 |
| Admin 扩展 | 投稿查询目前无分页，数据量增长后会一次读取全部记录 | 达到明确阈值后再增加服务端分页；当前规模不必提前复杂化 | 支撑未来数据增长 | 中 | P2（触发式） |

## 4. 推荐优先级

### P0：必须考虑

#### 1. 公开投稿与 Admin 登录的滥用防护

这是当前最实际的产品风险。公开投稿接口使用服务端 Secret 写入数据库，但没有流量与重复控制；Admin 登录也没有失败限制。建议优先做低摩擦、不可见的保护，不要一开始就引入重验证码。

#### 2. 投稿隐私与发布预期

用户提交的字段虽然没有姓名，但“学校 + 专业 + 城市 + 精确日期”仍可能缩小到个人。提交前必须让用户知道哪些内容会进入公开 Hall、会经过人工审核、如何申请修改。这是可信归档产品的基本责任。

#### 3. 8.6MB 东方标题字体

字体是目前最明确、无需构建测量也能确认的资源问题。它服务于品牌，但不应以 8.6MB 的全站首访成本存在。优先考虑字符子集，而不是移除视觉特色。

### P1：高价值优化

1. 在 Hall 首屏增加一行以内的用途与可信度摘要，不增加新板块。
2. 把样本量、快照日期、非实时/非官方性质从深层弹窗提升为轻量可见信息。
3. 让 View 的月份范围从数据派生，消除硬编码漂移。
4. 完善提交成功、失败和重复投稿的闭环。
5. 拆分 Hall/View 数据入口与客户端组件边界，避免同时发送两套数据。
6. 对大型 CSS Module 做按页面职责的有限拆分，不进行设计系统重写。
7. 修正移动端投稿 Dialog 的圆角/滚动结构，并提高小字号下限。
8. 补齐趋势表的可访问语义。
9. 完成页面级 description、中文 locale 和一张站点级 OG 图。
10. 为数据与审核边界增加小而关键的自动化测试。
11. 对齐 README、`.env.example` 与真实来源命名。

### P2：锦上添花

- Hall 深页浏览有真实需求后，再考虑页码入 URL、位置恢复和结果播报；
- 给 mailto 增加复制兜底；
- 使用真实数据更新时间生成 sitemap `lastModified`；
- 统一 404 与后台的中英文语气；
- 清理确认无用的 ThemeSwitcher、依赖和旧通用组件；
- 数据量达到阈值后再为 Admin 增加服务端分页；
- 公开传播需求明确后，再评估 Dataset 结构化数据。

### 不建议

- 不建议把 Hall 改为搜索优先或筛选优先的信息架构；97 条、且 11+ 已分页的当前规模不足以证明需要常驻筛选 UI。
- 不建议添加账号、收藏、关注、评论、通知或个人 Dashboard。
- 不建议增加更多排名维度、徽章、勋章、成长值或分享战绩；这会把档案展推向游戏化。
- 不建议把 View 扩展成多图表分析平台。当前分位数、月度趋势和案例已覆盖核心问题。
- 不建议前台实时读取 Supabase。静态快照与人工 Release 是可信度和可追溯性的优势。
- 不建议在 Admin 加“一键发布生产”按钮。审核、导出、验证、部署的人工边界符合当前风险等级。
- 不建议为了工程整洁进行全量 CSS 重写、状态库迁移或通用组件重建。
- 不建议增加持续环境动画、粒子、数字滚动、霓虹状态光或更强卡片边框。

## 5. 不建议增加的功能

### 搜索与复杂筛选

只有当用户反馈“找不到自己的案例”或深页浏览数据证明定位困难时，再考虑单一、临时的快速定位能力。不要默认增加地点、学校、专业、状态、日期等多维筛选器，否则 Hall 会快速变成 Dashboard。

### 用户系统

注册、登录、个人投稿中心、收藏和通知会引入身份、隐私、找回、删除与安全义务。当前“匿名浏览 + 轻量投稿 + 人工审核”更符合产品规模。

### 实时数据

实时并不天然更可信。Checkee 的价值来自经过审核、可追溯、可解释的快照。实时同步会削弱 Release 边界，并增加错误数据直接暴露的概率。

### 案例详情页

为每条案例创建独立页面会放大组合字段的可识别风险，也会制造大量低信息密度页面。备注 Popover 与列表内展开更适合当前数据粒度。

### 社交与游戏化

排行榜已有足够的仪式感，不需要点赞、评论、勋章、分享卡片生成器或“击败百分之多少用户”。这些功能既偏离数据归档，也可能把敏感经历娱乐化。

### 更多可视化

地图、热力图、趋势预测、等待时间预测器看似有价值，但当前样本是非实时、非完整人群。若没有更严格的数据口径与置信度表达，新增图表会制造超出数据能力的确定感。

## 6. 建议的产品化收尾顺序

```text
第一阶段：守住可信度
投稿/登录防滥用 → 投稿隐私说明 → 数据更新时间与样本性质

第二阶段：减轻首访与移动端摩擦
Hall 首访摘要 → 提交反馈闭环 → 390px Dialog/字号验证

第三阶段：降低技术负担
字体子集 → Hall/View 数据与客户端边界拆分 → 关键测试

第四阶段：准备公开传播
页面级 SEO 文案 → 中文 locale → 单张品牌 OG 图 → sitemap 时间语义
```

这一顺序不会改变 Checkee 的产品形态。它主要让现有体验更可信、更快、更清楚，也为后续数据增长留出安全空间。

## 7. 审查依据摘要

- 产品与架构基准：`docs/CHECKEE_STATE.md`
- Hall / View 页面入口：`app/page.tsx`、`app/view/page.tsx`
- 核心展示组件：`components/checkmate/checkmate-experience.tsx`
- 核心响应式样式：`components/checkmate/checkmate-experience.module.css`
- 投稿界面与客户端请求：`components/checkmate/submit-case-dialog.tsx`、`components/checkmate/submit-case.ts`
- 投稿服务端校验：`app/api/submissions/route.ts`
- Admin 鉴权与审核：`lib/admin/auth.ts`、`components/admin/submission-list.tsx`
- 数据类型与说明：`data/checkmate/types.ts`、`data/checkmate/config.ts`
- 发布与验证链路：`lib/admin/hall.ts`、`scripts/export-hall-master.py`、`scripts/verify-hall-data.py`
- SEO：`app/layout.tsx`、`app/seo.tsx`、`app/sitemap.ts`、`app/robots.ts`
- 工程与 CI：`package.json`、`.github/workflows/quality-check.yml`

---

**最终判断：** Checkee 当前最需要的不是更多 UI，而是把“可信归档”做实。优先守住投稿入口、隐私边界、数据解释和移动端可读性，再处理字体与客户端负载；搜索、账号、更多图表和游戏化功能均应继续克制。
